<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= markdown(post.body) %>
  </p>

  <p>
    On <%= post.created_at.strftime("%d/%b/%y")%>
    By <%= link_to post.user.username, post.user %>
  </p>

</div>
